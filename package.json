{
  "name": "facevault-backend",
  "version": "1.0.0",
  "description": "FaceVault Backend API",
  "main": "main.py",
  "scripts": {
    "start": "uvicorn main:app --host 0.0.0.0 --port 8000",
    "dev": "uvicorn main:app --reload",
    "serve": "serve -s . -p 8000"
  },
  "dependencies": {
    "serve": "^14.2.1"
  }
} 